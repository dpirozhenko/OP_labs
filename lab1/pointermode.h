#include <iostream>
#include <cstring>

#pragma once

char* enter_filename();
void pointermode(const char*);
void generate_input(const char*);
void read_file(const char*);
int countstrings(const char*);
void deleteOddStrings(const char*);
void strngs(const char*, const char*);
